function [t, s] = OPC_readOpc(file)% Function to read binary OPC files (extension opc) from the Southern% Surveyor%% Writen by CSIRO. For files from OPC_DAS use Jason's% function, OPC_read_d00.m%% [t, s] = readOpc read the binary file 'file' with extension 'opc' and% returns the time (seconds since midnightin 't' and% the associated bug size (um) in s.narginchk(1, 1);fid = fopen(file, 'r', 'b');if fid < 0    error(['Unable to open ', file]);endx = fread(fid, [2,inf], 'float32');t = x(1, :)';s = x(2, :)';